# Section Components

Final marketing sections should live here once implementation starts.

Use `.agents/CONTENT.md` for copy and fallbacks, `.agents/design.html` for component schemas, and keep animation-heavy sections isolated as client components.
