# Hooks

Add browser-only hooks here when GSAP, Swiper, media queries, or interactive state are introduced.

Keep hooks client-only and small. Server components should remain the default for route content.
